<%= form_for(@chosakekka_code) do |f| %>
  <% if @chosakekka_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chosakekka_code.errors.count, "error") %> prohibited this chosakekka_code from being saved:</h2>

      <ul>
      <% @chosakekka_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :atai %><br>
    <%= f.text_field :atai %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
